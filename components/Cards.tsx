import React from 'react';

// elements
import { Grid } from './elements';

// 메인 화면의 카드들을 보여주는 컴포넌트(4열)
const Cards = (props) => {
  return (
    <React.Fragment>
      카드
    </React.Fragment>
  )
}
export default Cards;